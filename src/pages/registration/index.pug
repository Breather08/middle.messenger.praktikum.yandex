.auth-page.registration.active-page
    .auth-card 
        .auth-card__title
            span Регистрация
        .auth-card__fields 
            include ../../components/text-field/index
            +textField('Почта', 'email')
            +textField('Логин')
            +textField('Имя')
            +textField('Фамилия', 'tel')
            +textField('Телефон')
            +textField('Пароль', 'password')
            +textField('Пароль (еще раз)', 'password')
        .auth-card__actions
            button.btn.btn_block.btn_success.btn_bold.btn__registration Зарегистрироваться
            .additional  
                a(href="#" id="has-account-link") Уже есть аккаунт?
            